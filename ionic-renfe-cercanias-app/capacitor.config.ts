import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-renfe-cercanias-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
